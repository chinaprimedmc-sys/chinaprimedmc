# Public Assets

Public assets are intentionally empty in the foundation phase.

Future rules:

- Use descriptive SEO file names.
- Prefer AVIF with WebP fallback.
- Store original source tracking in `content/assets-manifest`.
- Never reuse the same image role without documenting it.
